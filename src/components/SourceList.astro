---
const { sources = [] } = Astro.props;
---
<section class="source-list">
  <p class="source-list-title">引用元・参考資料</p>
  <ul>
    {sources.map((source) => (
      <li>
        <a href={source.url}>{source.title}</a>
        <span>{source.publisher}</span>
      </li>
    ))}
  </ul>
</section>
